// import * as actionTypes from '../constants/theGreatWarConstants'

export const $$initialState = {
}

export default function theGreatWarReducer ($$state = $$initialState, action) {
  switch (action.type) {
    default:
      return $$state
  }
}
